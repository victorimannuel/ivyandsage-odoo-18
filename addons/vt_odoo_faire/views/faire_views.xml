<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="faire_faire_view_form" model="ir.ui.view">
            <field name="name">faire.faire.view.form</field>
            <field name="model">faire.faire</field>
            <field name="arch" type="xml">
                <form string="Faire v2">
                    <header>
                        <button name="button_authorize" string="Authorize" class="btn-primary" type="object"/>
                        <field name="status" widget="statusbar"/>
                    </header>
                    <sheet>
                        <group>
                            <group>
                                <field name="active" widget="boolean_toggle"/>
                                <field name="name"/>
                                <field name="application_id"/>
                                <field name="secret_id" widget="password"/>
                                <field name="scope_ids" widget="many2many_tags"/>
                            </group>
                            <group>
                                <field name="date_request"/>
                                <field name="state"/>
                                <field name="authorization_url" widget="url"/>
                                <field name="authorization_code" widget="password"/>
                                <field name="oauth_access_token" widget="password"/>
                            </group>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
        <record id="faire_faire_view_list" model="ir.ui.view">
            <field name="name">faire.faire.view.list</field>
            <field name="model">faire.faire</field>
            <field name="arch" type="xml">
                <list string="Faire Integration">
                    <field name="name"/>
                    <field name="date_request"/>
                    <field name="status"/>
                    <field name="active" widget="boolean_toggle"/>
                </list>
            </field>
        </record>
        <record id="faire_faire_action" model="ir.actions.act_window">
            <field name="name">Faire Integration</field>
            <field name="res_model">faire.faire</field>
            <field name="view_mode">list,form</field>
            <field name="view_id" ref="faire_faire_view_list"/>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                Add a new account
                </p>
                <p>
                Set up a new faire integration
                </p>
            </field>
        </record>
    </data>
</odoo>